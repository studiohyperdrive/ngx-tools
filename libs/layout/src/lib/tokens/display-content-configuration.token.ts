import { InjectionToken } from '@angular/core';
import { NgxDisplayContentConfiguration } from '../types';
/**
 * A token to provide the necessary configuration to the directive
 */
export const NgxDisplayContentConfigurationToken =
	new InjectionToken<NgxDisplayContentConfiguration>('NgxDisplayContentConfiguration');
