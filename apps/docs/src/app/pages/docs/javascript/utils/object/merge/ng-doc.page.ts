import { NgDocPage } from '@ng-doc/core';
import { ObjectCategory } from '../../../../../../categories/javascript';

const mergePage: NgDocPage = {
	title: `merge`,
	mdFile: './index.md',
	category: ObjectCategory,
	order: 1,
};

export default mergePage;
