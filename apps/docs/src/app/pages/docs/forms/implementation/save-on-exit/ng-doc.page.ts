import { NgDocPage } from '@ng-doc/core';
import { FormsImplementationCategory } from '../../../../../categories';

const SaveOnExitPage: NgDocPage = {
	title: `Save on exit`,
	mdFile: './index.md',
	category: FormsImplementationCategory,
	order: 4,
};

export default SaveOnExitPage;
