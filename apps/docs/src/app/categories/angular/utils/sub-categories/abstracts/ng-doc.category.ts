import { NgDocCategory } from '@ng-doc/core';
import UtilsCategory from '../../ng-doc.category';

const AbstractsCategory: NgDocCategory = {
	title: 'Abstracts',
	category: UtilsCategory,
};

export default AbstractsCategory;
